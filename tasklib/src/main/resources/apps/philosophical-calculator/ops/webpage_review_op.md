---
specifies: ../webpage-notes.md
related:
  - ../page.html
  - ../content.md
task_type: DOCUMENT_CRITIQUE
---

* Review the rendered `page.html` as a reader would see it
* Look for: unreadable contrast, broken or overflowing layout, missing responsive
  behaviour, unstyled or duplicated sections, broken images and links, missing headings,
  missing semantic landmarks, and content present in `content.md` but absent from the page
* Rewrite **only** the `## OCR review` section of `webpage-notes.md`
  - reproduce the `## Author notes` section **verbatim**, ahead of your section
  - if `## Author notes` is missing, emit an empty one
* Format your section as a prioritized checklist of concrete, actionable fixes
* Say `- nothing to fix` when the rendering is clean — do not invent work