---
transforms: ../comic_(\d+)\.comic\.json -> ../comicbook.html
---

* Generate a self-contained HTML presentation of the comicbook
* All images should be referenced
* Captions should be displayed as text below the images - the images have captions, so the text should be muted
* Style the page appropriately to make it visually appealing
