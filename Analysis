--- a/webui/src/main/kotlin/com/simiacryptus/cognotik/plan/tools/reasoning/TemporalReasoningTask.kt
+++ b/webui/src/main/kotlin/com/simiacryptus/cognotik/plan/tools/reasoning/TemporalReasoningTask.kt
import com.simiacryptus.cognotik.webui.session.getChildClient
 import com.simiacryptus.cognotik.webui.session.SessionTask
import com.simiacryptus.cognotik.webui.session.getChildClient
 import org.slf4j.Logger
 import java.io.FileOutputStream
import java.nio.charset.StandardCharsets
import java.nio.charset.StandardCharsets
 import java.nio.file.FileSystems
import java.nio.file.Path
 import java.nio.file.Path
 import java.time.LocalDateTime
 import java.time.format.DateTimeFormatter
  private fun validateAndGetApi(
    orchestrationConfig: OrchestrationConfig,
    task: SessionTask,
    log: Logger,
    resultFn: (String) -> Unit
  ): ChatInterface? {
    return try {
      val typeConfig = typeConfig ?: throw RuntimeException("Type configuration not found")
      (typeConfig.model?.let { orchestrationConfig.instance(it) }
        ?: orchestrationConfig.defaultChatter).getChildClient(task)
    } catch (e: Exception) {
      val errorMsg = "Failed to initialize API: ${e.message}"
      log.error(errorMsg, e)
      task.error(e)
      resultFn(errorMsg)
      null
    }
  }
 
@@ -104,6 +105,36 @@ class TemporalReasoningTask(
     }
   }
 
+  private fun validateAndGetApi(
+    orchestrationConfig: OrchestrationConfig,
+    task: SessionTask,
+    log: Logger,
+    resultFn: (String) -> Unit
+  ): com.simiacryptus.cognotik.chat.model.ChatInterface? {
+    return try {
+      orchestrationConfig.defaultChatter
+    } catch (e: Exception) {
+      log.error("Failed to initialize API", e)
+      task.error(e)
+      resultFn("ERROR: Failed to initialize API - ${e.message}")
+      null
+    }
+  }
+
+  private fun getPriorCode(executionState: Map<String, Any>?): String {
+    return executionState?.entries?.joinToString("\n") { (key, value) ->
+      "$key: $value"
+    } ?: ""
+  }
+
+  private fun String.truncateForDisplay(maxLength: Int): String {
+    return if (this.length > maxLength) {
+      this.substring(0, maxLength) + "\n... (truncated)"
+    } else {
+      this
+    }
+  }
+
   data class TimelineEvent(
     val timestamp: String = LocalDate.now().format(DateTimeFormatter.ISO_DATE),
     val event_type: String = "generic",
@@ -152,6 +183,14 @@ class TemporalReasoningTask(
     }
   }
 
+  private fun SessionTask.safeComplete(message: String, log: Logger) {
+    try {
+      this.complete(message)
+    } catch (e: Exception) {
+      log.warn("Failed to complete task with message: $message", e)
+    }
+  }
+

   override fun run(
     agent: TaskOrchestrator,
     messages: List<String>,
import com.simiacryptus.cognotik.apps.general.renderMarkdown
import com.fasterxml.jackson.databind.ObjectMapper
                return "Warning: No seed items found to start crawling"
import com.simiacryptus.cognotik.apps.general.renderMarkdown
import com.simiacryptus.cognotik.chat.model.ChatInterface
import com.simiacryptus.cognotik.platform.model.ApiChatModel
    val api = validateAndGetApi(orchestrationConfig, task, log as Logger, resultFn) ?: return
          if (executionConfig?.identify_feedback_loops == true) appendLine("- ✅ Feedback Loops")
      transcript?.write(
        transcript?.write("## Context\n\n$priorContext\n\n$relatedContext\n\n---\n\n".toByteArray(StandardCharsets.UTF_8))
      transcript?.write("## System Structure\n\n$structureAnalysis\n\n---\n\n".toByteArray(StandardCharsets.UTF_8))
        transcript?.write("## Delays & Accumulations\n\n$delaysAnalysis\n\n---\n\n".toByteArray(StandardCharsets.UTF_8))
        transcript?.write("## System Archetypes\n\n$archetypesAnalysis\n\n---\n\n".toByteArray(StandardCharsets.UTF_8))
        transcript?.write("## Emergent Behavior\n\n$emergentAnalysis\n\n---\n\n".toByteArray(StandardCharsets.UTF_8))
        transcript?.write("## Leverage Points\n\n$leverageAnalysis\n\n---\n\n".toByteArray(StandardCharsets.UTF_8))
        transcript?.write("## Intervention Simulation\n\n${simulationResults.joinToString("\n\n")}\n\n---\n\n".toByteArray(StandardCharsets.UTF_8))
      transcript?.write("## Synthesis & Recommendations\n\n$synthesis\n\n---\n\n".toByteArray(StandardCharsets.UTF_8))
      transcript?.write(
      transcript?.write("\n\n## Error Occurred\n\n**Error:** ${e.message}\n\n**Type:** ${e.javaClass.simpleName}\n".toByteArray(StandardCharsets.UTF_8))
  private fun getPriorCode(executionState: Any?): String {
    return try {
      executionState?.toString()?.take(2000) ?: ""
    } catch (e: Exception) {
      log.warn("Error retrieving prior code context", e)
      ""
    }
  }
  private fun String.truncateForDisplay(maxLength: Int = maxDescriptionLength): String {
    return if (this.length > maxLength) {
      this.substring(0, maxLength) + "\n\n*... (truncated)*"
    } else {
      this
    }
  }
  private fun SessionTask.safeComplete(message: String, log: Logger) {
    try {
      this.complete(message)
    } catch (e: Exception) {
      log.warn("Failed to complete task", e)
    }
  }