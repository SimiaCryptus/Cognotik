# Generated Code
To calculate the sum of the first 100 integers, we can use Groovy's range and sum functions.


```groovy
def calculateSum(int limit) {
    def sum = (1..limit).sum()
    task.complete("The sum of the first $limit integers is: **$sum**", false, "div", "", true)
}

calculateSum(100)

```
