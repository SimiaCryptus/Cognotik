---
transforms:
  - index\.html -> social.png
related:
  - icon.png
task_type: GenerateImage
---

Create a social sharing preview image for this app — the card that appears when the
app's link is posted to a chat, feed, or message. DO NOT INCLUDE TEXT OR TEXT CHARACTERS.

* The composition must be **wide landscape**, roughly 1.91:1 (1200 × 630), so it fills
  an Open Graph / Twitter `summary_large_image` card without cropping.
* Center a single bold, immediately readable visual motif that represents the app's
  purpose as described in the README. One clear idea, not a collage.
* Keep the outer ~10% of every edge visually calm; platforms crop and overlay these
  margins. Nothing essential may live near the borders.
* Use high contrast and saturated, confident color so the image stays legible as a
  small thumbnail on a phone.
* Match the app's mood and color family so the social card, icon, and background read
  as one family.
* Clean, modern, uncluttered. Generous negative space. No busy detail that dissolves
  when scaled down.
* DO NOT INCLUDE TEXT OR TEXT CHARACTERS.
* No glyphs, letterforms, numerals, logos, watermarks, UI chrome, or signage.
* Do not render, write, imply, or cause any language-readable imagery.
* High quality and optimized for web use so it loads quickly on any device.
* NO TEXT.