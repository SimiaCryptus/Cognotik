there is no way to save edited files. there should be a save and save all menu item with shortcut keys at least
single clicking a file currently opens it... sometimes. this is confusing and over-reactive. double clicks open files.
double clicking folders should expand them
the breadcrumb ui is poorly formatted and confusing in function. let's just remove it.
the theme selector does not color itself according to theme and looks terrible in dark mode
