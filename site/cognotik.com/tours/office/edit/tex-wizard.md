# TX Wizard — AI-Powered LaTeX Document Authoring

A walkthrough of the complete TX Wizard workflow: writing notes, generating LaTeX source, building a PDF, and iterating with updates.

---

## Introduction

Welcome to TX Wizard, an AI-powered LaTeX document authoring and PDF rendering pipeline. In this demo, we'll walk through the complete workflow — writing notes, generating LaTeX source, building a PDF, and iterating with updates.

---

## 1. Logging In

We need to log in first. *(Credentials entered.)* Logged in successfully — now we can see the app hub.

---

## 2. Launching TX Wizard

This is the Cognotech App Hub where all available applications are listed. Let's find and launch TX Wizard. Here's TX Wizard — let's open it.

---

## 3. Interface Overview

TX Wizard has three main panels:

- **Left panel** — Inputs for notes, update notes, and style directives
- **Center panel** — The pipeline with four steps: Render TX, Update TX, Build PDF, and Rebuild
- **Right panel** — The output: generated LaTeX source, PDF preview, and usage statistics

---

## 4. Configuring AI Models

First, let's configure the AI models. We'll select models for smart processing, fast processing, and image handling.

---

## 5. Writing Document Content

Now let's write our document content. I'll enter some notes describing a project status report with key metrics, technical highlights, and next steps. *(Notes entered.)*

Notes saved. These will be used as the source content for our LaTeX document.

---

## 6. Style Directives

We can also provide style directives to control the document's appearance — things like document class, packages, fonts, and formatting preferences. *(Style directives entered.)*

---

## 7. Rendering LaTeX Source

Now for the exciting part — let's render the LaTeX source. The AI will transform our notes into a properly structured LaTeX document. *(Processing...)*

The LaTeX source has been generated. Let's take a look at what was produced.

Here's the generated LaTeX source. Notice how the AI has structured our notes into proper LaTeX with sections, formatting, and the packages we requested. The source is fully editable if we want to make manual adjustments.

---

## 8. Building the PDF

With our LaTeX source ready, let's compile it into a PDF. This runs the build script which invokes the LaTeX compiler. *(Building...)*

The PDF has been built successfully. Let's see the result.

Here's our compiled PDF. The document looks clean and professional, with properly formatted sections, metrics, and technical content — all generated from our plain text notes.

We can also check the build log to see the compilation output and verify there were no warnings or errors.

---

## 9. Updating the Document

Now let's demonstrate the update workflow. Suppose we want to:

- Add a risk section
- Convert the metrics into a table
- Add a footnote

I'll enter these changes as update notes. *(Update notes entered.)*

Update notes saved. The AI will use these along with the existing document to produce a revised version.

Let's run the Update TX step. This will intelligently modify the existing LaTeX source based on our update notes. *(Processing...)*

The document has been updated. The AI incorporated our requested changes while preserving the existing structure.

---

## 10. Rebuilding the PDF

Let's rebuild the PDF to see our changes reflected in the final output. *(Building...)*

The updated PDF is ready. You can see the new risks section, the metrics table, and the footnote have all been added.

---

## 11. Autofix Feature

TX Wizard also includes an autofix feature. If the LaTeX compilation encounters errors, the rebuild step can automatically diagnose and fix them. Let's demonstrate this capability. *(Autofix rebuild running...)*

The autofix rebuild is complete. Any compilation issues would have been automatically detected and resolved.

---

## 12. Token Usage Statistics

Let's check the token usage statistics to see how much AI processing was used during our session.

---

## 13. Downloading Output

Finally, we can download both the LaTeX source and the compiled PDF for use outside the application.

---

## Summary

And that's the complete TX Wizard workflow. We went from plain text notes to a polished PDF document, then iterated with updates — all powered by AI. The pipeline handles LaTeX generation, compilation, and even automatic error fixing. Thank you for watching.


